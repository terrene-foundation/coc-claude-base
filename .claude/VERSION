{
  "name": "coc-claude-base",
  "version": "2.45.6",
  "type": "coc-use-template",
  "description": "Language-agnostic, non-Kailash CO/COC USE template (variant: base)",
  "synced_at": "2026-06-29T08:28:38Z",
  "upstream": {
    "name": "loom",
    "type": "coc-source",
    "version": "2.45.0",
    "loom_sha": "e2ac475",
    "synced_at": "2026-06-29T08:28:38Z",
    "template_version": "2.45.0",
    "build_version": "2.45.0",
    "source_commit": "e2ac475"
  },
  "variant": "base",
  "tier_subscriptions": [
    "cc",
    "co",
    "onboarding"
  ],
  "changelog": [
    {
      "version": "2.45.6",
      "date": "2026-06-29",
      "summary": "Gate-2 base sync from loom v2.45.0 (loom_sha e2ac475). Managed .gitignore block grew +8 per-clone trust-state entries (coordination-log.jsonl* + codify-lease/coordination-mode/observations/violations/provenance/posture/.initialized siblings) per sync-manifest gitignore_additions; CLI baselines (AGENTS.md/GEMINI.md) + .codex/.gemini emitted trees refreshed for current rule set (cc-artifacts.md, self-referential-codify.md). base build=null: no SDK pins, no uv/cargo steps."
    },
    {
      "date": "2026-06-26",
      "version": "2.45.5",
      "summary": "loom@9101e34 narrow propagation (base CC-only, loom 2.45.0 unchanged — 9755c2d..9101e34: PR #685 added .claude/codex-templates/ + .claude/gemini-templates/ to sync-manifest::obsoleted, loom-internal emitter SOURCE trees that must never reach a consumer surface). NO content re-emit: sync-tier-aware --verify base --all-templates CONSISTENT (1056/1056 in-plan). Required change: .coc-obsoleted regenerated (Step 4.5 union of obsoleted+use_obsoleted) so codex-templates/+gemini-templates/ now appear; additive (every prior entry preserved). Both template dirs already ABSENT on base disk (base was always clean; the F1 redteam MED finding was on rs templates only) — this propagation keeps downstream /sync-from-template consumers informed. CC-only (clis:[claude]) — NO Codex/Gemini emission. build:null — NO SDK pins.",
      "changes": [
        "OBSOLETED (Step 4.5): .coc-obsoleted regenerated from sync-manifest.yaml::obsoleted (58) + use_obsoleted (13), dedup union = 69 path entries (was 64 unique / 66 lines). NEW this cycle: .claude/codex-templates/ + .claude/gemini-templates/ (PR #685) + .claude/commands/sync.md trio (already manifest-declared, folded in additively). ADDITIVE PROOF: 0 prior entries dropped (comm -23 empty); prior file's internal duplicate pair (terrene-naming/independence) collapsed to single entries.",
        "VERIFY: sync-tier-aware.mjs --target base --all-templates --verify CONSISTENT (coc-claude-base + coc-base, 1056 in-plan each; no obsoleted path present on disk). verify-overlays.sh base Failing:0 (rules/agents.md base slot, both templates). codex-templates/+gemini-templates/ grep present in regenerated .coc-obsoleted.",
        "CONTENT: no .claude/ content re-emit this cycle (9755c2d..9101e34 delta is the 13-line manifest obsoleted addition only). visibility=public preserved across marker rewrite.",
        "CC-ONLY: clis:[claude] — NO Codex/Gemini emission; no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin emission. CLAUDE.md preserved (template-owned).",
        "PINS: N/A (build:null — non-Kailash base; SDK-pin/BUILD-proposal steps correctly skipped; base templates stack-agnostic, no pin change expected or applied).",
        "BUMP: template 2.45.4->2.45.5; loom_sha 9755c2d->9101e34; source_commit->9101e34; upstream loom 2.45.0 unchanged."
      ]
    },
    {
      "date": "2026-06-26",
      "version": "2.45.4",
      "summary": "regenerated by /sync from loom@9755c2d + 2.45.0 — F1 distribution: Shard-A/B/C rule narrowing+extraction, F5 base re-tier, dogfood catch-up",
      "changes": [
        "DISTRIBUTE: sync-tier-aware.mjs --target base --template coc-claude-base (orchestrator-applied + --verify CONSISTENT). 1055 copied, 1 overlay (rules/agents.md base slot), 0 variant_only, 0 purged. Base tier [cc,co,onboarding]; coc-tier excluded by design (no Kailash specialists).",
        "CC-ONLY (clis:[claude]): NO Codex/Gemini emission; no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin leak. CLAUDE.md preserved (template-owned).",
        "GUARD: 0 Kailash coc-tier specialist/skill leaks. 0 operator-PII, 0 scripts/hooks refs, 0 loom-internal path leaks.",
        "PINS: N/A (build:null — non-Kailash base; SDK-pin/BUILD-proposal steps correctly skipped; sdk_packages unchanged/absent).",
        "BUMP: template 2.45.3->2.45.4; loom_sha 8197257->9755c2d; source_commit->9755c2d; upstream loom 2.45.0 unchanged."
      ]
    },
    {
      "date": "2026-06-25",
      "version": "2.45.3",
      "summary": "loom@8197257 Gate-2 distribution (base CC-only, loom 2.45.0) — MO-OPT W1: the multi-operator coordination substrate is now OPT-IN. PAYLOAD landed for base lane (W1 lives in tier-independent .claude/hooks/**, so it ships to base): new lib hooks/lib/coordination-mode.js (isCoordinationEnabled 5-tier precedence predicate); isCoordinationEnabled early-return-to-passthrough hoists in integrity-guard.js + journal-write-guard.js + signing-mutation-guard.js + hooks/lib/codify-lease.js + hooks/lib/operator-id.js; top-level hook adjacency-leasecheck.js. WHY: un-enrolled non-Kailash base consumers (solo operators, no roster) now get clean passthrough — pre-W1, four PreToolUse guards fired on independent preconditions a never-enrolled repo could not satisfy (integrity-guard's codify-branch test fired unconditionally on main/feat/*), disrupting every solo base consumer. Receipts: journal/0330 + journal/0331. Base subscribes [cc,co,onboarding]; coc-tier excluded by design (BASE-LANE CONTENT GUARD: 0 Kailash specialist/skill additions). CC-only (clis:[claude]) — NO Codex/Gemini emission. build:null — NO SDK pins.",
      "changes": [
        "DISTRIBUTE: sync-tier-aware.mjs --target base --template coc-claude-base. copied=943, verified 943/943 delivered (13 content-equal after BUILD-internal strip, 930 byte-equal), overlay_applied=1 + overlay_verified byte-equal (rules/agents.md base slot), variant_only=0, purged=0 (use_obsoleted absent on disk; manual Step-4.5 BOTH-halves purge removed 1: commands/cli-audit.md), skipped loom_local=2 + exclude=997 + use_exclude=29 + no_tier_match=538.",
        "W1 PAYLOAD (tier-independent hooks): hooks/lib/coordination-mode.js NEW; isCoordinationEnabled hoists in integrity-guard.js + journal-write-guard.js + signing-mutation-guard.js + hooks/lib/codify-lease.js + hooks/lib/operator-id.js; adjacency-leasecheck.js top-level. W1-landing verification PASS (coordination-mode.js present + isCoordinationEnabled grep in integrity-guard.js).",
        "OBSOLETED: .coc-obsoleted regenerated (66 entries = union obsoleted + use_obsoleted). Step-4.5 BOTH-halves purge ran (tier tool does use_obsoleted only).",
        "CONTENT GUARD: 0 Kailash coc-tier specialist additions. CC-only (clis:[claude]) — no Codex/Gemini emission; no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin leak.",
        "VERIFY: verify-overlays.sh base Failing:0. settings.json hook integrity verified; 0 scripts/hooks refs; 0 operator-PII; description (language-agnostic base) preserved.",
        "PINS: N/A (build:null — non-Kailash base; no SDK pins; SDK-pin/BUILD-proposal steps correctly skipped).",
        "BUMP: template 2.45.2->2.45.3; loom_sha f7c236d->8197257; upstream loom 2.45.0 unchanged."
      ]
    },
    {
      "date": "2026-06-23",
      "version": "2.45.2",
      "summary": "loom@f7c236d redistribution (base CC-only, loom 2.45.0 unchanged — c1e52a62..f7c236d catch-up: global cc/co rule updates agents.md + artifact-flow.md + zero-tolerance.md + self-referential-codify.md + multi-operator-coordination.md + wave-loop.md, commands inspect/maintain/release/sync-to-use, tier-independent hooks/bin). Base tier [cc,co,onboarding] — no coc-tier Kailash specialists; the large c1e52a62..f7c236d delta is mostly coc-tier ecosystem/capability substrate skipped by design (no_tier_match=538).",
      "changes": [
        "DISTRIBUTE: sync-tier-aware.mjs --target base --template coc-claude-base. copied=940, verified 940/940 (14 content-equal after BUILD-internal strip, 926 byte-equal), overlay_applied=1 + overlay_verified byte-equal (rules/agents.md base slot), variant_only=0, purged=0 (49 obsoleted absent on disk), skipped loom_local=2 + exclude=971 + use_exclude=29 + no_tier_match=538.",
        "CONTENT GUARD: 0 Kailash coc-tier specialist additions. CC-only (clis:[claude]) — no Codex/Gemini emission; no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin leak.",
        "VERIFY: hook integrity 38/38; 0 scripts/hooks refs; 0 operator-PII; description (language-agnostic base) preserved. verify-overlays.sh coc-claude-base Failing:0.",
        "PINS: N/A (build:null — non-Kailash base; no SDK pins; Step 6 + Step 8 SDK-pin/BUILD-proposal steps correctly skipped).",
        "BUMP: template 2.45.1->2.45.2; loom_sha c1e52a62->f7c236d; upstream loom 2.45.0 unchanged."
      ]
    },
    {
      "date": "2026-06-17",
      "version": "2.45.1",
      "summary": "loom@c1e52a62 redistribution (base CC-only, loom 2.45.0 unchanged) — sync-upflow Wave-3 todo-14: FIRST real /sync-to-use carrying the Wave-1/2 sync-family artifacts. Base tier [cc,co,onboarding] — no coc-tier Kailash specialists. Inbox ensure-exists provisioned.",
      "changes": [
        "INGEST: sync-family artifacts reach the base surface byte-equal (869 globals; 1 overlay; verify-overlays.sh base 2/2). Tier-filtered: no Kailash coc-tier specialists.",
        "INBOX: .claude/.proposals/inbox/{README.md,.gitkeep} ensure-exists provisioned.",
        "PINS: N/A (non-Kailash base template — no SDK pins).",
        "BUMP: template 2.45.0->2.45.1; loom_sha 6d18fe9->c1e52a62."
      ]
    },
    {
      "date": "2026-06-12",
      "version": "2.45.0",
      "summary": "loom@6d18fe9 Gate-2 distribution (base cc-only, loom 2.45.0) — MultiEdit-removal canonicalization (PR #500, journals 0276+0277). Claude Code removed the MultiEdit tool; stale MultiEdit(...) permissions.deny entries fired session-start warnings. PAYLOAD landed for base lane: settings.json (10 MultiEdit deny entries removed, PreToolUse/PostToolUse matchers -> Edit|Write|NotebookEdit; target-owned language-agnostic-base description preserved); hooks/lib/tool-classes.js (MUTATION_TOOLS keeps MultiEdit as runtime legacy tolerance) + github-login.js (GITHUB_LOGIN_FIELD_NAMES SSOT +3 fields); rules/multi-operator-coordination.md (§7 + MUST-6 live-matcher prose) + self-referential-codify.md (tool-classes.js added to Rule-2 allowlist). codex-mcp-guard runtime files NOT emitted (cc-only-legacy, clis:[claude]). Base subscribes [cc,co,onboarding]; coc-tier excluded (BASE-LANE CONTENT GUARD: 0 Kailash specialist/skill additions). CC-only — NO Codex/Gemini emission. build:null — NO SDK pins. Consumers MUST NOT re-add MultiEdit to settings matchers/permissions.",
      "changes": [
        "DISTRIBUTE: sync-tier-aware.mjs --target base --template coc-claude-base. copied=844, verified 844/844 (14 content-equal after BUILD-internal strip, 830 byte-equal), overlay_applied=1 + overlay_verified byte-equal (rules/agents.md base slot), variant_only=0, purged=0 (13 active-purge paths absent), skipped loom_local=2 + exclude=885 + use_exclude=29 + no_tier_match=514.",
        "settings.json: 10 MultiEdit(...) deny entries removed; both matchers Edit|Write|NotebookEdit; description stem language-agnostic base preserved.",
        "verify-overlays.sh base: 2/2 passing 0 failing. visibility=public preserved. .gitignore noop (10 entries 586B)."
      ]
    },
    {
      "date": "2026-06-12",
      "version": "2.44.0",
      "summary": "loom@1b26e91 Gate-2 distribution (base cc-only, loom 2.44.0) — Gate-1 rs-ingest cycle (kailash-rs appended_changes_2026_06_11, PR #496). GLOBAL rule extensions to zero-tolerance.md (Rule 3e binding-inheritance) + value-prioritization.md (MUST-2 SHA-pinned anchors) + worktree-isolation.md (Rule 2a re-assert cwd) + the zero-tolerance extract guide + skills/30-claude-code-patterns (worktree-orchestration + closure-parity). testing.md x3 extensions are coc-tier (base-excluded by design — testing.md is NOT in base's [cc,co,onboarding] subscription). Base subscribes [cc,co,onboarding]; coc-tier excluded (BASE-LANE CONTENT GUARD: 0 coc-tier specialist additions). CC-only (clis:[claude]) — NO Codex/Gemini emission. build:null — NO SDK pins.",
      "changes": [
        "DISTRIBUTE: sync-tier-aware.mjs --target base --template coc-claude-base. copied=844, verified 844/844 delivered (14 content-equal after BUILD-internal strip, 830 byte-equal), overlay_applied=1 + overlay_verified=1 (rules/agents.md base slot-composed byte-equal), variant_only=0, purged=0 (13 active-purge paths absent on disk), skipped loom_local=2 + exclude=881 + use_exclude=29 + no_tier_match=514. .gitignore noop (10 entries 586B). visibility=public preserved.",
        "CHANGED THIS CYCLE (loom 2.44.0 rs-ingest globals, base lane — 6 modifications, 0 additions): rules/zero-tolerance.md + rules/value-prioritization.md + rules/worktree-isolation.md + guides/rule-extracts/zero-tolerance.md + skills/30-claude-code-patterns/worktree-orchestration.md + skills/30-claude-code-patterns/closure-parity-specialist-discipline.md. All are cc/co-tier globals base subscribes to. rules/agents.md base overlay re-composed byte-identical (no agents.md change this cycle).",
        "CONTENT GUARD: 0 Kailash coc-tier specialist additions (git status: all 6 changes status M, zero status A). No agents/frameworks/ or skills/01-05-kailash on disk. testing.md correctly ABSENT (coc-tier, not in base subscription) — the v2.44.0 testing.md extensions do not reach the base lane.",
        "CC-ONLY: clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission; NO multi-cli scaffold (cc-only-legacy symlinks:[], manifest_distribute:false). Verified no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin leak.",
        "SDK pins: n/a (build:null — language-agnostic base; sdk_packages {}). Step 9/10 dependency-install skipped (lane declares no BUILD source).",
        "STEP 7.5: no .github/coc-sdk-refs-allowlist.txt (no contamination gate) — skipped (legitimate absence).",
        "VERIFY: verify-overlays.sh base 2/2 PASS (Failing: 0). Hook integrity 38/38; 0 scripts/hooks; 0 operator-PII; .coc-obsoleted 49 entries, 0 present on disk.",
        "PRESERVED: CLAUDE.md (template-owned); .claude/learning/** + .proposals/** + .coc-sync-marker never synced.",
        "BUMP: VERSION 2.43.0->2.44.0; loom_sha d7ec0d2->1b26e91; upstream.version 2.43.0->2.44.0."
      ]
    },
    {
      "date": "2026-06-12",
      "version": "2.43.0",
      "summary": "loom@d7ec0d2 Gate-2 distribution (base cc-only, loom 2.43.0) — #491 agents.md worktree-cluster extraction + security.md global slot-partition (PR #493, loom main 6590005). Base subscribes [cc,co,onboarding]; coc-tier excluded by design (BASE-LANE CONTENT GUARD: 0 coc-tier leaks across all 103 manifest coc-tier path patterns; the eatp-reference skill is co-tier, correctly included). CC-only (clis:[claude]) — NO Codex/Gemini emission. build:null — NO SDK pins.",
      "changes": [
        "DISTRIBUTE: sync-tier-aware.mjs --target base --template coc-claude-base. copied=844, verified 844/844 delivered (14 content-equal after BUILD-internal strip, 830 byte-equal), overlay_applied=1 + overlay_verified=1 (rules/agents.md base slot-composed byte-equal), variant_only=0, purged=0 (13 active-purge paths absent on disk), skipped loom_local=2 + exclude=880 + use_exclude=29 + no_tier_match=514. .gitignore noop (10 entries 586B). visibility=public preserved.",
        "CHANGED THIS CYCLE (#491, base lane): rules/agents.md base overlay re-composed (worktree MUST cluster extracted to skill; base overlay carries one-line MUST bullets + restores the binding-scoped-shard-PR clause); rules/security.md re-partitioned global slots (base lane carries the global Redactor Contract). verify-overlays.sh coc-claude-base: variant-applied(base,slot) PASS.",
        "CONTENT GUARD: 0 Kailash coc-tier specialist leaks (checked copied set against all 103 manifest coc-tier path patterns: agents/frameworks/**, skills/01-05-kailash, framework rules — none present). skills/26-eatp-reference is co-tier (CO methodology layer), correctly included per base [cc,co,onboarding] subscription.",
        "CC-ONLY: clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission; NO multi-cli scaffold (cc-only-legacy symlinks:[], manifest_distribute:false). Verified no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin leak.",
        "SDK pins: n/a (build:null — language-agnostic base; sdk_packages {}). Step 9/10 dependency-install skipped (lane declares no BUILD source).",
        "STEP 7.5: no .github/coc-sdk-refs-allowlist.txt (no contamination gate) — skipped (legitimate absence).",
        "PRESERVED: CLAUDE.md (template-owned); .claude/learning/** + .proposals/** + .coc-sync-marker never synced.",
        "BUMP: VERSION 2.42.1->2.43.0; loom_sha 1d6968e->d7ec0d2; upstream.version 2.42.1->2.43.0."
      ]
    },
    {
      "date": "2026-06-11",
      "version": "2.42.1",
      "summary": "loom@1d6968e Gate-2 completion re-sync (base cc-only, loom 2.42.1). PATCH cycle: loom merged PR #489 (prose trims to global rules agents/evidence-first-claims/zero-tolerance/security — the rs-variant change does NOT touch base) + the 2.42.1 VERSION bump. Base subscribes [cc,co,onboarding]; coc-tier excluded by design. CC-only (clis:[claude]) — NO Codex/Gemini emission. build:null — NO SDK pins.",
      "changes": [
        "DISTRIBUTE: sync-tier-aware.mjs --target base --all-templates. coc-claude-base: copied=844, verified 844/844 delivered (14 content-equal after BUILD-internal strip, 830 byte-equal), overlay_applied=1 + overlay_verified=1 (rules/agents.md base slot-composed byte-equal), variant_only=0, purged=0 (13 active-purge paths absent on disk; full union 49 written to .coc-obsoleted), skipped loom_local=2 + exclude=8000 + use_exclude=29 + no_tier_match=514. .gitignore noop (10 entries 586B). visibility=public preserved.",
        "CHANGED THIS CYCLE (PR #489 global-rule prose trims, base lane): rules/evidence-first-claims.md + rules/security.md + rules/zero-tolerance.md. rules/agents.md base overlay re-composed (byte-identical — PR #489 agents.md edit was rs-variant-only). MUST/BLOCKED/DO-NOT counts byte-identical loom-source vs landed (evidence-first=12, security=29, zero-tolerance=20).",
        "OVERLAY: rules/agents.md base slot-keyed overlay composed in-process (mode:slot) + stripped; verify-overlays.sh base: variant-applied(base,slot) PASS, Failing: 0.",
        "CC-ONLY: clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission; NO multi-cli scaffold (cc-only-legacy symlinks:[], manifest_distribute:false). Verified no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin leak.",
        "SETTINGS: state-file deny-matrix hardening preserved from loom source; (language-agnostic base) description preserved (Step 6b: no Python/Rust/Ruby tag for base). Hook integrity verified; 0 scripts/hooks; 0 operator-PII.",
        "SDK pins: n/a (build:null — language-agnostic base; sdk_packages {}). Step 9/10 dependency-install skipped (lane declares no BUILD source).",
        "STEP 7.5: no .github/coc-sdk-refs-allowlist.txt (no contamination gate) — skipped (legitimate absence).",
        "PRESERVED: CLAUDE.md (template-owned); .claude/learning/** + .proposals/** + .coc-sync-marker never synced.",
        "BUMP: VERSION 2.42.0->2.42.1; loom_sha 036c8ff->1d6968e; upstream.version 2.42.0->2.42.1."
      ]
    },
    {
      "date": "2026-06-11",
      "version": "2.42.0",
      "summary": "loom@036c8ff Gate-2 distribution (base cc-only, loom 2.42.0) — Gate-1 ingest cycle 2026-06-11. Base subscribes [cc,co,onboarding]; coc-tier artifacts excluded by design. NEW baseline rules evidence-first-claims.md + verify-claims-before-write.md (both cc/co tier, distributed); compacted agents/autonomous-execution/git/zero-tolerance/security/repo-scope-discipline/communication; NEW guides/rule-extracts/autonomous-execution.md; trust-posture + self-referential-codify edits. CC-only (clis:[claude]) — NO Codex/Gemini emission. build:null — NO SDK pins.",
      "changes": [
        "DISTRIBUTE: sync-tier-aware.mjs --target base --all-templates. coc-claude-base: copied=844, verified 844/844 byte-equal, stripped=15 (build-internal refs), overlay_applied=1 + overlay_verified=1 (rules/agents.md base slot-composed), purged=0 (all 13 obsoleted paths absent on disk), skipped no_tier_match=514 + exclude=8000 + use_exclude=29 + loom_only=9 + loom_local=2. visibility=public preserved.",
        "NEW baseline rules this cycle (cc/co tier — distributed to base): evidence-first-claims.md + verify-claims-before-write.md. Compacted: agents/autonomous-execution/git/zero-tolerance/security/repo-scope-discipline/communication. NEW guide guides/rule-extracts/autonomous-execution.md. trust-posture.md + self-referential-codify.md allowlist/trigger edits.",
        "OVERLAY: rules/agents.md base slot-keyed overlay composed in-process (mode:slot) + stripped. verify-overlays.sh base: variant-applied(base,slot) PASS, Failing: 0.",
        "CC-ONLY: clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission; NO multi-cli scaffold (cc-only-legacy symlinks:[], manifest_distribute:false). Verified no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin leak.",
        "SETTINGS: state-file deny-matrix hardening preserved from loom source; (language-agnostic base) description preserved (Step 6b: no Python/Rust/Ruby tag for base). Hook integrity 38/38; 0 scripts/hooks; 0 operator-PII.",
        "SDK pins: n/a (build:null — language-agnostic base; sdk_packages {}). Step 9/10 dependency-install skipped (lane declares no BUILD source).",
        "STEP 7.5: no .github/coc-sdk-refs-allowlist.txt (no contamination gate) — skipped (legitimate absence).",
        "PRESERVED: CLAUDE.md (template-owned); .claude/learning/** + .proposals/** + .coc-sync-marker never synced.",
        "BUMP: VERSION 2.41.0->2.42.0; loom_sha bbfa970->036c8ff; upstream.version 2.41.0->2.42.0."
      ]
    },
    {
      "date": "2026-06-11",
      "version": "2.41.0",
      "summary": "loom@bbfa970 redistribution (base cc-only, loom 2.41.0) — version-currency stamp. Base subscribes [cc,co,onboarding]; the loom 2.41.0 coc-tier skill 36-claude-managed-agents is NOT distributed here (no coc subscription).",
      "changes": [
        "822 globals verified (15 stripped + 807 byte-equal) + 1 base overlay byte-equal. No new content this cycle (CMA is coc-tier, base-excluded by design).",
        "SETTINGS: state-file deny-matrix hardening from loom source (F123/FSUB); (language-agnostic base) label preserved. 38/38 hooks resolve.",
        "SDK pins: n/a (build:null — language-agnostic base).",
        "BUMP: upstream.version 2.40.0->2.41.0 (loom_sha bbfa970)."
      ]
    },
    {
      "date": "2026-06-11",
      "version": "2.40.0",
      "summary": "loom@57c88cc redistribution (base cc-only, loom 2.40.0) — FSUB signed-emitter wiring + 1daa45b..57c88cc catch-up.",
      "changes": [
        "FSUB: signed journal-slot-reservation + codify-lease emission (knowledge-convergence MUST-2/3 writer half). New hooks/lib/coc-emit.js + codify-lease.js + journal-reserve.js + journal-body-anchor.js + coordination-log.js default-registration; operator-id.js carried current. codify.md NOT carried (coc tier — base subscribes [cc,co,onboarding]).",
        "CATCH-UP (1daa45b..57c88cc): full expected-state diff beyond FSUB — strip-build-internal.mjs + sync-tier-aware.mjs + provenance-event.js + 8 rule-extracts guides + cli-audit.md + rules (security/value-prioritization/cross-cli-artifact-hygiene/knowledge-convergence/self-referential-codify) + 3 30-claude-code-patterns skills + audit fixtures. 822 globals verified (15 stripped + 807 byte-equal) + 1 base overlay byte-equal.",
        "SDK pins: n/a (build:null — language-agnostic base; sdk_packages null).",
        "BUMP: VERSION 2.40.0 ; loom_sha 57c88cc ; upstream.version 2.40.0. PR #480/#481, loom main 57c88cc."
      ]
    },
    {
      "date": "2026-06-10",
      "version": "2.39.0",
      "summary": "REDISTRIBUTION — coc-claude-base advanced from loom@c445a66 to loom@1daa45b (both 2.39.0; same-version REDISTRIBUTION bringing the base lane current to loom HEAD, no loom version bump). Base lane (variant: base, tier_subscriptions: [cc, co, onboarding], build: null — NO SDK pins). CC-only template (clis:[claude], baseline_files:[CLAUDE.md]) — NO Codex/Gemini emission. base does NOT subscribe to coc tier. Canonical sync-tier-aware.mjs --target base --all-templates: copied=820, verified 820/820 byte-equal, purged=0, skipped no_tier_match=513 (coc-tier exclusion) + exclude=855 + use_exclude=29 + loom_local=2. OVERLAY FIX: rules/agents.md base slot-keyed overlay re-applied as strip(compose(global, variants/base)) — the tier-aware raw-copy shipped the loom global (drift-from-both, md5 271190d); corrected to composed+stripped (md5 c9b6b62, single strip rewrite: packages/kailash-ml/src path neutralized in Example 7). verify-overlays.sh base: 2/2 PASS, Failing: 0. settings.json unchanged: hook integrity 38/38; 0 scripts/hooks; 0 operator-PII; description (language-agnostic base) correct. .gitignore noop (10 entries). NO BUILD source (build:null) — sdk_packages {}. NO .github/coc-sdk-refs-allowlist.txt (no contamination gate — Step 7.5 skipped).",
      "changes": [
        "REDISTRIBUTION: loom globals raw-copied via sync-tier-aware.mjs --all-templates (copied=820, verified 820/820 byte-equal). base tiers=[cc,co,onboarding]; coc-tier files skipped (no_tier_match=513).",
        "OVERLAY FIX: rules/agents.md base slot-keyed overlay re-applied as strip(compose(global, variants/base)). tier-aware raw-copy shipped the loom global (drift-from-both 271190d); corrected to composed+stripped (md5 c9b6b62, matches sister coc-base PASS). verify-overlays.sh base: variant-applied(base,slot) PASS, Failing: 0.",
        "SETTINGS: unchanged by sync. Hook integrity 38/38; 0 scripts/hooks refs; 0 operator-PII; description (language-agnostic base) correct.",
        "SDK PINS: build:null — no pyproject.toml/Cargo.toml; sdk_packages {} (Step 5 skipped).",
        "STEP 7.5: no .github/coc-sdk-refs-allowlist.txt (no contamination gate) — skipped (legitimate absence).",
        "CC-ONLY: clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission.",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten); .claude/learning/** + .proposals/** + .coc-sync-marker never synced."
      ]
    },
    {
      "date": "2026-06-01",
      "version": "2.39.0",
      "summary": "MINOR UPSTREAM REFRESH — coc-claude-base advanced from loom@1de1c41/2.38.0 to loom@e934018/2.39.0. Base lane (variant: base, tier_subscriptions: [cc, co, onboarding], build: null — NO SDK pins). Carries PR #425's converged global-tier artifacts: #411 F101-3 journal author-discipline (new rule journal-author-discipline.md + provenance-author-backing.js hook + journal-write-guard.js author-backing branch + decision-recording-discipline skill + audit-fixtures/journal-author-discipline/), F110 governed-throughput refresh of agents.md, plus trust-posture.md + self-referential-codify.md allowlist extensions for the new rule/hook surface. The #423 rb->rs all-bindings collapse is rs-lane-only and does NOT touch base. CC-only template (clis:[claude], baseline_files:[CLAUDE.md]) — NO Codex/Gemini emission. base does NOT subscribe to coc tier (no Kailash framework specialists/skills); codex-architect/gemini-architect skip/no_tier_match (correct). Canonical sync-tier-aware.mjs --target base --template coc-claude-base: copied=755, purged=0, skipped no_tier_match=528 (coc-tier exclusion) + exclude=764 + use_exclude=29 + loom_only=8 + loom_local=2. OVERLAY FIX: rules/agents.md base slot-keyed overlay re-applied as strip(compose(global, variants/base)) — the tier-aware raw-copy initially shipped the loom global (drift-from-both); corrected to the composed+stripped form matching the sister coc-base PASS. verify-overlays.sh base: 2/2 PASS, Failing: 0. settings.json unchanged: hook integrity 22/22; 0 scripts/hooks; 0 operator-PII; description (language-agnostic base) correct for base variant. .coc-obsoleted regenerated (union obsoleted + use_obsoleted). NO BUILD source (build:null) — sdk_packages {}. NO .github/coc-sdk-refs-allowlist.txt (no contamination gate — Step 7.5 skipped). Disclosure preflight (scan-synced-disclosure --check exit 0) + HALT-on-dirty-tracked (clean) PASS.",
      "changes": [
        "REFRESH: loom globals raw-copied via sync-tier-aware.mjs (copied=755). base tiers=[cc,co,onboarding]; coc-tier files skipped (no_tier_match=528).",
        "NEW (PR #425 / #411 F101-3): journal-author-discipline.md rule + provenance-author-backing.js hook + decision-recording-discipline.md skill + audit-fixtures/journal-author-discipline/ landed (4 new paths).",
        "MODIFIED (PR #425): agents.md (F110 governed-throughput), security.md, journal.md, self-referential-codify.md + trust-posture.md (allowlist + emergency-trigger extensions for the new rule/hook), emit.mjs, scan-synced-disclosure.mjs, journal-write-guard.js.",
        "OVERLAY FIX: rules/agents.md base slot-keyed overlay re-applied as strip(compose(global, variants/base)). tier-aware raw-copy had shipped the loom global (drift-from-both); corrected to composed+stripped (md5 matches sister coc-base PASS). verify-overlays.sh base: variant-applied(base,slot) PASS.",
        "OBSOLETED: .coc-obsoleted regenerated (union obsoleted + use_obsoleted, deduped). All obsoleted paths confirmed absent on disk.",
        "SETTINGS: unchanged by sync. Hook integrity 22/22; 0 scripts/hooks refs; 0 operator-PII; description (language-agnostic base) correct (base is its own variant, not py/rs/rb).",
        "SDK PINS: build:null — no pyproject.toml/Cargo.toml; sdk_packages {} (Step 5 skipped per build:null early-exit semantics).",
        "STEP 7.5: no .github/coc-sdk-refs-allowlist.txt (no contamination gate) — skipped (legitimate absence).",
        "CC-ONLY: clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission. NO emit.mjs --lang headroom step. headroom_pct n/a.",
        "PRESERVED: CLAUDE.md (template-owned, never overwritten); .claude/learning/** + .proposals/** + .coc-sync-marker never synced."
      ]
    },
    {
      "date": "2026-06-01",
      "version": "2.37.0",
      "summary": "MINOR UPSTREAM REFRESH — coc-claude-base advanced from loom@6bd4075/2.31.0 to loom@7a5e8be/2.37.0. Base lane (variant: base, tier_subscriptions: [cc, co, onboarding], build: null — NO SDK pins). Range carries loom v2.32.0–v2.37.0 incl FB (2.37.0: independence.md + terrene-naming.md removed from COC distribution — Terrene-internal messaging governance, not codegen-orchestration; purged + obsoleted-listed) + #407/base-axis version-utils.js KNOWN_TEMPLATE_REPOS fix (now carries kailash-coc-py/kailash-coc-rs/coc-base/coc-claude-base slugs; tier-independent .claude/hooks/**) + F112 (codex-architect/gemini-architect/cc-audit/skill-+command-authoring) + F113 (governed-throughput.md path-scoped). CC-only template (clis:[claude], baseline_files:[CLAUDE.md]) — NO Codex/Gemini emission. base does NOT subscribe to coc tier (no Kailash framework specialists/skills); codex-architect/gemini-architect skip/no_tier_match (correct). Canonical sync-tier-aware.mjs --target base --template coc-claude-base: copied=743, verified 743/743 byte-equal, purged=0, skipped no_tier_match=528 (coc-tier exclusion). Overlay: rules/agents.md base slot-keyed overlay re-applied (strip(compose)) — fixes prior drift-from-both; verify-overlays.sh coc-claude-base: variant-applied(base,slot) PASS. #407 version-utils.js grep -c=4. settings.json hook integrity 22/22; 0 scripts/hooks; 0 operator-PII; description normalized (Python)->(language-agnostic base). .coc-obsoleted regenerated (46 entries). NO BUILD source (build:null) — sdk_packages {}. NO .github/coc-sdk-refs-allowlist.txt (no contamination gate — Step 7.5 skipped). Disclosure preflight + pre-sync freshness PASS.",
      "changes": [
        "REFRESH: loom globals raw-copied via sync-tier-aware.mjs (copied=743, verified 743/743 byte-equal). base tiers=[cc,co,onboarding]; coc-tier files skipped (no_tier_match=528).",
        "#407 + BASE-AXIS: version-utils.js KNOWN_TEMPLATE_REPOS now includes terrene-foundation/{kailash-coc-py,kailash-coc-rs,coc-base,coc-claude-base}. grep -c=4 (was 0).",
        "OVERLAY FIX: rules/agents.md base slot-keyed overlay re-applied as strip(compose(global, variants/base)). Fixes pre-existing drift-from-both. verify-overlays.sh: variant-applied(base,slot) PASS.",
        "FB (loom 2.37.0): independence.md + terrene-naming.md confirmed gone (purged); now on global obsoleted: list.",
        "OBSOLETED: .coc-obsoleted regenerated (46 entries = union obsoleted + use_obsoleted, deduped).",
        "SETTINGS: description normalized (Python)->(language-agnostic base) — base is non-Kailash language-agnostic; prior (Python) was an inherited loom-source residual. Hook integrity 22/22; 0 scripts/hooks; 0 operator-PII.",
        "SDK PINS: build:null — no pyproject.toml/Cargo.toml; sdk_packages {} (Step 5 skipped per /sync-to-build build:null early-exit semantics).",
        "STEP 7.5: no .github/coc-sdk-refs-allowlist.txt (no contamination gate) — skipped (legitimate absence).",
        "CC-ONLY: clis:[claude], baseline_files:[CLAUDE.md]. NO Codex/Gemini emission. NO emit.mjs --lang headroom step. headroom_pct n/a.",
        "PRESERVED: CLAUDE.md (template-owned); .claude/learning/** + .proposals/** + .coc-sync-marker never synced. gitignore created (10 entries, was absent)."
      ]
    }
  ],
  "updated": "2026-06-29"
}
