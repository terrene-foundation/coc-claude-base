# COC Sync Marker — coc-claude-base (base lane, cc-only-legacy)
timestamp: 2026-06-26T23:00:53Z
source: loom/.claude
loom_sha: 9101e34
variant: base
clis: [claude]
visibility: public
stats:
  copied: 0            # narrow propagation — no .claude/ content re-emit (--verify CONSISTENT)
  variant_only: 0
  overlay_applied: 0   # rules/agents.md base slot byte-equal from prior cycle (verify-overlays Failing:0)
  verify_failures: 0
  purged: 0            # codex-templates/+gemini-templates/ already ABSENT on base disk (base always clean)
  obsoleted_union: 69  # regenerated .coc-obsoleted (was 64 unique / 66 lines); +codex-templates/+gemini-templates/+sync.md trio, additive
  preserved: [CLAUDE.md, .proposals/, learning/, settings.local.json]
  baselines_emitted:
    cc: 0          # CLAUDE.md preserved, not emitted
note: |
  loom@9101e34 narrow propagation (base cc-only, loom 2.45.0 unchanged — 9755c2d..9101e34).
  HEADLINE: PR #685 added .claude/codex-templates/ + .claude/gemini-templates/ to
  sync-manifest::obsoleted (loom-internal emitter SOURCE trees; an F1-distribution-redteam MED
  finding on the rs templates where a v2.31.0 catch-up had committed them — base templates were
  ALWAYS clean). This is a manifest-propagation cycle, NOT a content re-emit. sync-tier-aware.mjs
  --target base --all-templates --verify reported CONSISTENT (1056/1056 in-plan, no obsoleted
  path present on disk). REQUIRED CHANGE: .coc-obsoleted regenerated (Step 4.5 union of
  obsoleted+use_obsoleted) — dedup union now 69 path entries (was 64 unique / 66 lines), with
  .claude/codex-templates/ + .claude/gemini-templates/ now present. ADDITIVE: 0 prior entries
  dropped (comm -23 empty); the prior file's internal duplicate terrene-naming/independence pair
  collapsed to single entries; +sync.md trio folded in (already manifest-declared). Both template
  dirs absent on disk → 0 purged here; the .coc-obsoleted entry keeps downstream
  /sync-from-template consumers able to purge them. CC-only (clis:[claude]) — NO Codex/Gemini
  emission; no .codex/.gemini/.codex-mcp-guard/AGENTS.md/GEMINI.md/bin emission. CLAUDE.md
  preserved (template-owned). verify-overlays.sh base Failing:0. build:null — no SDK pins
  (base stack-agnostic; no pin change). VERSION 2.45.4->2.45.5; loom_sha 9755c2d->9101e34;
  upstream loom 2.45.0 unchanged.
